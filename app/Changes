android {
    namespace "com.example.makbites"
    compileSdk flutter.compileSdkVersion
    ndkVersion flutter.ndkVersion

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_8
        targetCompatibility JavaVersion.VERSION_1_8
    }

    kotlinOptions {
        jvmTarget = "1.8" // مقدار درست برای JVM Target
    }

    defaultConfig {
        applicationId "com.example.makbites"
        minSdk 23
        targetSdk flutter.targetSdkVersion
        versionCode flutter.versionCode
        versionName flutter.versionName
        multiDexEnabled true // پشتیبانی از MultiDex
    }

    buildTypes {
        release {
            signingConfig signingConfigs.debug
            minifyEnabled false
            shrinkResources false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }

    // پشتیبانی از MultiDex و حل مشکل ناسازگاری کتابخانه‌ها
    packagingOptions {
        resources {
            excludes += "/META-INF/{AL2.0,LGPL2.1}"
        }
    }
}

dependencies {
    implementation platform('com.google.firebase:firebase-bom:32.0.0')
    implementation 'com.google.firebase:firebase-auth'
    implementation 'com.google.firebase:firebase-firestore'
    implementation 'com.google.firebase:firebase-analytics'
    implementation 'com.google.firebase:firebase-storage'

    // MultiDex برای پروژه‌های دارای بیش از 64k متد
    implementation 'androidx.multidex:multidex:2.0.1'

    // حل مشکل FlutterActivity
    implementation 'io.flutter:flutter_embedding:1.0.0'
}

// پلاگین‌های مورد نیاز
apply plugin: 'com.google.gms.google-services'
apply from: "$flutterRoot/packages/flutter_tools/gradle/flutter.gradle" // مهم برای هماهنگی با فلاتر
